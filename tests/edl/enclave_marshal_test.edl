enclave {
	from "sgx_tstdc.edl" import sgx_oc_cpuidex;
    trusted {
		/* define ECALLs here. */
		public int enclave_marshal_test_init([user_check]void** zone_config, [out]void** zone);		
		public void enclave_marshal_test_destroy(
			[user_check] void* zone);

        public int test([user_check] void** data_in, [user_check] void** data_out);
    };

    untrusted {
    };
};
